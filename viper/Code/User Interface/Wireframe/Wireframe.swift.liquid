//
//  {{ prefix }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

class {{ module_info.name }}Wireframe {
    
    // Other wireframes
    // TO ADD
    
    var presentedViewController: UIViewController?
    
    var presenter: {{ module_info.name }}Presenter?
    var dataManager: {{ module_info.name }}DataManager?
    
    func present{{ module_info.name }}From(viewController: UIViewController) {
        let moduleViewController = get{{ module_info.name }}ViewController()
        moduleViewController.eventHandler = presenter
        presenter?.userInterface = moduleViewController
        viewController.present(moduleViewController, animated: true, completion: nil)
        presentedViewController = moduleViewController
    }
    
    func dismiss{{ module_info.name }}() {
        presentedViewController?.dismiss(animated: true, completion: nil)
    }
    
    func get{{ module_info.name }}ViewController() -> {{ module_info.name }}ViewController {
        return {{ module_info.name }}ViewController(nibName: {{ module_info.name }}ViewController.nibName, bundle: nil)
    }
    
}
